<ul class="nav nav-tabs">
  <li class="<%='active' if post.ptype == TWEET_TYPE %>"><a href="#tweet" data-toggle="tab">Add a Note</a></li>
  <li class="<%='active' if post.ptype == LINK_TYPE %>"><a href="#link" data-toggle="tab">Post a Link</a></li>
  <li class="<%='active' if post.ptype == BLOG_TYPE %>"><a href="#blog" data-toggle="tab">Write a new Blog Post</a></li>
  <!--<li><a href="#folder" data-toggle="tab">Create a new Folder</a></li>/-->
</ul>
<div class="tab-content">
  <div class="tab-pane <%='active' if post.ptype == TWEET_TYPE %>" id="tweet">
    <%= form_for post do |f| %>
      <%= f.hidden_field :parent_id, value: parent_id %>
      <%= f.hidden_field :ptype, value: TWEET_TYPE %>

      <%= f.label "Note title:", class: 'form-label' %>
      <%= f.text_field :title, :autofocus=>true, class: 'form-control' %>

      <%= f.label "Note message:", class: 'form-label' %>
      <%= f.text_area :body, placeholder: "(Not required)", class: 'form-control' %>

      <%= f.radio_button :private, true, checked: true %>
      <%= f.label "Private" %>
      <%= f.radio_button :private, false %>
      <%= f.label "Public" %>
      <br/>
      <%= f.submit "Save", class: 'btn btn-success'%>
    <% end %>
  </div>
  <div class="tab-pane <%='active' if post.ptype == LINK_TYPE %>" id="link">
    <%= form_for post do |f| %>
      <%= f.hidden_field :parent_id, value: parent_id %>
      <%= f.hidden_field :ptype, value: LINK_TYPE %>

      <%= f.label "Title for Link" %>
      <%= f.text_field :title, :autofocus=>true, class: 'form-control' %>

      <%= f.label "URL:"%>
      <%= f.text_field :body, placeholder: "http://", class: 'form-control' %>

      <%= f.radio_button :private, true, checked: true %>
      <%= f.label "Private" %>
      <%= f.radio_button :private, false %>
      <%= f.label "Public" %>
      <br/>
      <%= f.submit "Save", class: 'btn btn-success' %>
    <% end %>
  </div>
  <div class="tab-pane <%='active' if post.ptype == BLOG_TYPE %>" id="blog">
    <%= form_for post do |f| %>
      <%= f.hidden_field :parent_id, value: parent_id %>
      <%= f.hidden_field :ptype, value: BLOG_TYPE %>

      <%= f.label "Blog Title:" %>
      <%= f.text_field :title, :autofocus=>true, class: 'form-control' %>

      <%= f.label "Blog Content:"%>
      <%= f.text_area :body, rows: 15, placeholder: "Type your text here.", class: 'form-control' %>

      <%= f.radio_button :private, true, checked: true %>
      <%= f.label "Private" %>
      <%= f.radio_button :private, false %>
      <%= f.label "Public" %>

      <br/>
      <div class="">
        <a data-toggle="collapse" data-parent="#accordion" href="#collapseOne">
          Click here to show instructions on how to format your blog post
        </a>
      </div>
      <br/>
      <div id="collapseOne" class="collapse ">
        <ul>
          <li>To list bullet points like this, start each line with an asterisk (*)</li>
          <li>Surround *your text* with asterisks (*) to <i>make it italic</i></li>
          <li>Surround **your text** with double asterisks (**) to <b>make it bold</b></li>
          <li>To include a link use brackets and parentheses: [link text](www.linkURL.com)</li>
          <li>Surround ~~your text~~ with double tildes to add <del>strikethrough</del> </li>
        </ul>
        <br/>
      </div>

      <%= f.submit "Save", class: 'btn btn-success' %>  
    <% end %>
  </div>
</div>